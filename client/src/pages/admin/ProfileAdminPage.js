import React from "react";

const ProfileAdminPage = ({ current }) => {
	return (
		<div
			style={{
				minHeight: "100vh",
				backgroundColor: "red",
			}}
		>
			<div>profile</div>
		</div>
	);
};

export default ProfileAdminPage;
